import { NextResponse } from 'next/server'
import { assertAdmin } from '@/lib/cms-auth'
import { loadCmsRelationalSnapshot, saveCmsRelational } from '@/lib/cms-relational-save'
export async function GET(request:Request){const access=await assertAdmin(request);if('error'in access)return access.error;try{const x=await loadCmsRelationalSnapshot(access.adminClient,'faq');const [categories,catalog]=await Promise.all([access.adminClient.from('support_faq_categories').select('id,name,slug,description,image_path,image_alt,sort_order,is_active').order('sort_order'),access.adminClient.from('catalog_categories').select('id,name,slug').eq('status','active').order('display_order')]);if(categories.error||catalog.error)throw new Error('Unable to load FAQ categories.');return NextResponse.json({section:x.parent??{section_key:'global_support_faq',title:'Frequently Asked Questions',subtitle:''},items:x.items,categories:categories.data??[],catalogCategories:catalog.data??[],revision:x.revision},{headers:{'Cache-Control':'no-store'}})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:'Unable to load this section.'},{status:500})}}
export async function POST(request:Request){const access=await assertAdmin(request);if('error'in access)return access.error;return saveCmsRelational(access,'faq',await request.json().catch(()=>null))}

